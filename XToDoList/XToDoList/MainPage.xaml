<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XToDoList"
             x:Class="XToDoList.MainPage">

    <StackLayout Orientation="Vertical">
        <ListView x:Name="ToDoList" 
                  ItemsSource="{Binding ToDoItems, Mode=OneWay}">
            <!--ItemSelected="{Binding SelectedToDoItem}"/>-->
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <StackLayout Orientation="Vertical">
                                <Label Text="{Binding Title, Mode=OneWay}"/>
                                <Label Text="{Binding Description, Mode=OneWay}"/>
                            </StackLayout>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackLayout Orientation="Horizontal">
            <Button Text="Click me1"/>
            <Button Text="Click me2"/>
        </StackLayout>

    </StackLayout>


</ContentPage>
